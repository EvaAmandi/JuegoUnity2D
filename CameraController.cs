using UnityEngine;

public class CameraController : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is createdTr
    public Transform personaje;
    private float tamañoCamara;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
